# API Configuration
# Note: With proxy configuration, these URLs are no longer needed
# The proxy will handle routing to the backend server

# Development: Vite proxy routes /api/* to http://87.201.193.142:8000/*
# Production: Vercel proxy routes /api/* to http://87.201.193.142:8000/*

# Legacy configuration (not used with proxy setup)
# VITE_API_BASE_URL=http://87.201.193.142:8000
# VITE_API_BASE_URL=https://your-backend-domain.com